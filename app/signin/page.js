'use client';
import {useState} from 'react';
import {supabase} from '../../lib/supabase';
import Link from 'next/link';
export default function Signin(){const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [name,setName]=useState('');const [signup,setSignup]=useState(false);const [msg,setMsg]=useState('');async function submit(e){e.preventDefault();setMsg('');if(!supabase){setMsg('Supabase is not configured in this deployment yet.');return}if(signup){const {error}=await supabase.auth.signUp({email,password,options:{data:{full_name:name}}});setMsg(error?.message||'Account created. Check your email if confirmation is enabled.')}else{const {error}=await supabase.auth.signInWithPassword({email,password});setMsg(error?.message||'Signed in successfully. Open your account.')}}return <div className="page narrow"><div className="eyebrow">BG Smart Services</div><h1>{signup?'Create account':'Sign in'}</h1><form className="card form" onSubmit={submit}>{signup&&<label>Name<input value={name} onChange={e=>setName(e.target.value)} required/></label>}<label>Email<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></label><label>Password<input type="password" value={password} onChange={e=>setPassword(e.target.value)} minLength={6} required/></label><button className="btn" type="submit">{signup?'Create account':'Sign in'}</button>{msg&&<div className="notice">{msg}</div>}<button className="text-button" type="button" onClick={()=>setSignup(!signup)}>{signup?'Already have an account? Sign in':'Need an account? Create one'}</button><Link className="text-link" href="/marketplace">Continue shopping →</Link></form></div>}
