'use client';
import {useEffect,useState} from 'react';import {supabase} from '../../../lib/supabase';import Link from 'next/link';import {ArrowLeft,Save,UserRound} from 'lucide-react';
export default function PersonalInformation(){const[user,setUser]=useState(null),[name,setName]=useState(''),[phone,setPhone]=useState(''),[email,setEmail]=useState(''),[msg,setMsg]=useState(''),[busy,setBusy]=useState(true);useEffect(()=>{(async()=>{if(!supabase){setMsg('Supabase is not configured.');setBusy(false);return}const{data:{user}}=await supabase.auth.getUser();if(!user){window.location.href='/signin';return}setUser(user);setEmail(user.email||'');const{data}=await supabase.from('profiles').select('full_name,phone').eq('id',user.id).maybeSingle();setName(data?.full_name||user.user_metadata?.full_name||'');setPhone(data?.phone||user.user_metadata?.cellphone||user.phone||'');setBusy(false)})()},[]);async function save(e){e.preventDefault();if(!user)return;setBusy(true);setMsg('');const{error:authError}=await supabase.auth.updateUser({email:email.trim()||undefined,phone:phone.trim()||undefined,data:{full_name:name.trim()}});if(authError){setMsg(authError.message);setBusy(false);return}const{error}=await supabase.from('profiles').upsert({id:user.id,full_name:name.trim()||null,phone:phone.trim()||null,updated_at:new Date().toISOString()});setMsg(error?error.message:'Your personal information has been saved.');setBusy(false)}return <main className="settings-page"><div className="settings-shell"><Link href="/account" className="settings-back"><ArrowLeft size={17}/> Account</Link><div className="settings-hero"><span className="settings-icon"><UserRound size={22}/></span><div><div className="eyebrow">Account settings</div><h1>Personal Information</h1><p>Keep your contact details up to date for orders and delivery.</p></div></div><form className="settings-card settings-form" onSubmit={save}><label>Full name<input value={name} onChange={e=>setName(e.target.value)} placeholder="Your full name" required/></label><label>Email address<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required/><small>Changing your email may require confirmation.</small></label><label>Cellphone number<input type="tel" value={phone} onChange={e=>setPhone(e.target.value)} placeholder="e.g. 071 234 5678"/></label>{msg&&<div className="notice">{msg}</div>}<button className="btn btn-primary" disabled={busy}><Save size={17}/>{busy?'Saving…':'Save changes'}</button></form></div></main>}