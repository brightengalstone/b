'use client';
import {useEffect,useState} from 'react';
import Link from 'next/link';
export default function Cart(){const [items,setItems]=useState([]);useEffect(()=>setItems(JSON.parse(localStorage.getItem('bg_cart')||'[]')),[]);const subtotal=items.reduce((s,x)=>s+x.price*x.quantity,0);const deliveryFee=65;const total=subtotal+deliveryFee;function clear(){localStorage.removeItem('bg_cart');setItems([])}return <div className="page"><div className="eyebrow">BG Smart Services</div><h1>Cart</h1>{!items.length?<div className="card"><h2>Your cart is empty</h2><p>Browse local products and add something to your order.</p><Link className="btn" href="/marketplace">Back to Marketplace</Link></div>:<><div className="list">{items.map(x=><div className="item" key={x.id}><div><b>{x.name}</b><p>Qty {x.quantity} • R{x.price.toFixed(2)} each</p></div><b>R{(x.price*x.quantity).toFixed(2)}</b></div>)}</div><div className="summary card"><div><span>Subtotal</span><b>R{subtotal.toFixed(2)}</b></div><div><span>Delivery</span><b>R65.00</b></div><hr/><div className="total"><span>Total</span><b>R{total.toFixed(2)}</b></div><div className="actions"><Link className="btn" href="/checkout">Checkout</Link><button className="btn alt" onClick={clear}>Clear cart</button></div></div></>}</div>}
